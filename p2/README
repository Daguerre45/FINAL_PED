En un primer lugar se quería realizar esta práctica mediante el lenguaje JavaScript.
Pero debido a que las tuberías solo se pueden utilizar dentro del mismo sistema operativo. Sin embargo, se puede utilizar el protocolo HTTP para crear una aplicación similar.
